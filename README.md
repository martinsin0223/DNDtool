# 战争迷雾地图工具

一个给 DND / TRPG 跑团 DM 使用的单文件网页地图工具。打开网页就能用,玩家看着同一块屏幕,DM 在上面一点点擦开战争迷雾、移动 Token、亮出线索。

**在线使用：**  
[https://martinsin0223.github.io/DNDtool/](https://martinsin0223.github.io/DNDtool/)

当前版本：**v3.24.0**

## 最新版本：v3.24.0

v3.24.0 把词条快捷键的区域动作升级为 **切换区域显示**：绑定揭示区域的词条按一下会揭示区域,再按一次会把同一片区域覆盖回去。它只影响这个词条绑定的区域,仍然走迷雾撤销链路,适合现场手滑按早后快速补救。

---

## 一分钟先试一下

如果你不想先读说明,可以直接照这 5 步:

1. 打开上面的在线地址。
2. 点顶栏「编辑」→「加载地图」,选一张 PNG / JPG / WebP。
3. 选择「全部覆盖」让整张地图先盖上迷雾。
4. 点「回到跑团」。
5. 用顶栏的「揭」笔刷在地图上一点点擦,玩家就看到了。

试完觉得有意思,再继续往下读怎么把它用进真实跑团。

---

## 这个工具解决什么问题

线下跑团 DM 有一张地图,玩家围着电视、投影或共享笔记本一起看。DM 需要稳定、快速、可控地揭开玩家应该看到的区域,以及在地图上放 Token、线索、标记。

工具就是为这一件事设计的。

### 它坚持的几个原则

- **地图优先**：地图永远是屏幕主角,UI 只服务现场操作,不抢戏。
- **DM 手动控制**：不做自动视野、不做触发器、不做规则判定。该亮哪一块由你决定。
- **共享屏幕可信**：默认是"玩家也能看的现场态",不暴露隐藏对象列表,不暴露 DM 备忘。
- **本地优先**：不需要登录,不需要服务器,网页打开就能用。
- **静态部署**：公开版只有 `README.md`、`USAGE.md` 和 `index.html`,可以放在 GitHub Pages 这类静态托管上,也可以下载到本地双击打开。
- **可搬家**：本机保存、导出场景包、私有云存档三条路径并存,换电脑、出门跑团、回家继续都能接得上。

### 它不做什么

- 不做玩家账号,不做 DM / 玩家权限分离。
- 不做多人实时同步(玩家不能远程看,远程跑团请用 VTT)。
- 不做自动视野(谁看见什么由 DM 决定)。
- 不做规则自动化(没有掷骰、没有 HP、没有状态条)。
- 不导出 PNG 成图(需要图就用系统截图)。
- 不依赖后端、React、npm、CDN 或任何外部资源。

这些不是暂时没做,而是产品边界。如果你需要其中任何一项,你需要的应该是完整 VTT,不是这个工具。

### 它适合谁

- 线下跑团的 DM
- 用电视、投影、平板或共享电脑展示地图的桌面团
- 想要"比截图灵活,但比完整 VTT 轻很多"的地图展示工具
- 需要提前准备多场景、事件点、Token、文字标注、图片标注和遮罩的战役主持人
- 跑团时需要根据玩家拿到的情报,一键揭开预设地图区域的 DM

---

## 功能总览(速查表)

| 功能 | 说明 |
|---|---|
| 双层迷雾 | 地形层和事件层分离控制 |
| 笔刷揭示 / 覆盖 | 按当前层擦开或补回迷雾 |
| 全揭笔刷 | 同时揭开地形层和事件层 |
| 多场景 | 每个场景独立地图、迷雾、Token、事件和设置 |
| 全部场景导出 / 导入 | 用 `.fogbundle.json` 转移整套跑团进度 |
| 本机自动保存 | 浏览器 IndexedDB 自动保留进度 |
| 云存档 | 用自己的私有 GitHub 仓库上传 / 读取 / 恢复历史 |
| 超大地图占位恢复 | 太大的地图不上传图片本体,只同步遮罩和对象,换电脑后重新绑定 |
| 云端地图库 | 从私有仓 `manual-maps/` 选择同尺寸大图绑定占位场景 |
| 云端地图库索引 | 可用 `manual-maps/index.json` 提前显示尺寸、大小和备注 |
| 事件点 | 放置机关、线索、遭遇、地点说明 |
| Token | 放置玩家、敌人、NPC 或棋子；探索 Token 可拖动清雾 |
| 文字标注 | 在地图上放地点名、说明、路线 |
| 图片标注 | 在地图上放小图、徽记、战术图标 |
| 词条聚光 | 临时显露 / 高亮同词条对象 |
| 词条快捷键 | A-Z 执行聚光或切换绑定区域显示 |
| 揭示区域 | 预设网格区域,跑团时一键揭开(不是自动触发器) |
| 网格遮罩 | 六边形 / 方形网格,按整格揭示或覆盖 |
| 网格校准 | 调整网格大小、位置、旋转 |
| 对象面板 | 编辑态搜索、筛选、定位、编辑对象 |
| 事件快切 | 编辑态快速搜索事件并切换事件层显示状态 |
| Checkpoint | 保存迷雾恢复点,便于现场回滚两层遮罩 |
| 章节信息 | 每个场景可编辑章节序号和标题 |

---

## 详细使用手册

完整操作流程、准备步骤、跑团现场动作、跨设备恢复和快捷键见 [USAGE.md](USAGE.md)。

## 数据与隐私

- 本地进度保存在当前浏览器的 IndexedDB。
- GitHub Pages 只提供静态网页。
- 工具没有项目作者维护的服务器。
- 不使用账号系统。
- 不会把地图自动上传到公开仓库。
- `.fogbundle.json` 不包含地图原图。
- 如果使用云存档,状态和普通地图会上传到**你自己控制的**私有 GitHub 仓库。
- 清理浏览器站点数据会删除本机保存的项目。

---


## 浏览器支持

推荐:Chrome、Edge、Firefox、Safari、iPad Safari。

iPhone 小屏可以打开,但不是主要优化目标。

---

## 离线使用

如果不想联网,可以下载仓库里的 `index.html`,用浏览器直接打开。

离线时:

- 主功能仍可用。
- 本机保存仍可用。
- 导出 / 导入仍可用。
- **云存档不可用**(因为需要访问 GitHub API)。

---

## 当前限制

- 不导出 PNG 成图,需要图片时请用系统截图。
- 不做多人实时同步。
- 不做玩家权限模式。
- 不做自动视野、触发器、传送门等电子游戏逻辑。
- 单张地图最大边长约 4096 像素,超过会自动降采样。
- 标注按对象中心点判断是否被遮罩,不做逐像素裁切。
- 一个词条如果绑定多个揭示区域,一次切换会产生多个撤销项,撤销时可能要按多次。
- 跑团态的揭示区域列表会显示区域名和格数,区域命名应避免剧透。

---

## 技术特点

- 单文件 `index.html`
- 无后端
- 无构建步骤
- 无 npm 依赖
- 无 CDN 依赖
- 可直接部署到 GitHub Pages
- 主要数据保存在浏览器 IndexedDB
- 支持导出 JSON 场景包

---

## 版本记录

### v3.24.0

词条快捷键的区域动作升级为切换区域显示。

- 原"揭示区域"快捷键效果改名为"切换区域显示"。
- 第一次按下会揭示词条绑定区域。
- 区域已经打开时再次按下,会覆盖同一片区域。
- 只影响该词条绑定的揭示区域,不全图覆盖。
- 不保存额外开关状态,直接读取当前遮罩像素判断揭示或覆盖。
- 手动区域列表里的"揭示"按钮仍保持单向揭示。

### v3.23.0

新增探索 Token。

- Token 编辑里可开启“探索 Token”。
- 可用半径滑块设置清雾范围。
- 探索 Token 拖动结束后清除地形层和事件层遮罩。
- 普通 Token 默认不清雾。
- 清雾动作复用 fog undo 栈，一次拖动一次撤销。
- 不做视野系统、墙体遮挡、光照、朝向、玩家权限或多人同步。

### v3.22.0

词条快捷键新增"快捷键效果"设置。

- 默认仍是聚光,兼容旧词条和旧存档。
- 可把某个词条的快捷键设为区域动作,按键后直接揭示它绑定的区域。
- 区域快捷键复用原有迷雾撤销链路,可用 `Ctrl/Cmd + Z` 回滚。

### v3.21.1

修复在“新建/编辑揭示区域”弹窗中点击“开始选择”时,被弹窗状态误判为不在编辑态的问题。v3.21.0 是维护版本,无公开用户行为变化。

### v3.20.1

修复从"揭示区域"列表中新建或编辑区域时,被弹窗状态误拦截的问题。

### v3.20.0

新增"揭示区域"。

主要变化:

- 编辑态可按网格预设区域。
- 区域可绑定词条。
- 跑团态可从区域列表手动揭示。
- 词条聚光中可一键揭示当前词条绑定区域。
- 支持揭示地形层、事件层或双层。
- 揭示动作支持撤销。
- 区域保存网格快照,避免后续网格校准导致旧区域漂移。
- 导出、导入、bundle 和云存档状态链路都会保留揭示区域。

边界:

- 不做每格复杂属性数据库。
- 不做自动触发器。
- 不做玩家权限系统。
- 不做多人实时同步。

### v3.19.1

修复占位地图恢复向导的残留失败路径。

主要变化:

- 跨场景切换失败后清理恢复目标。
- 云端地图库绑定失败或尺寸不符后清理恢复目标。
- 避免后续普通加载地图被旧占位恢复状态劫持。

### v3.19.0

新增占位地图恢复向导。

主要变化:

- 编辑态集中查看所有待绑定占位场景。
- 支持跨场景直接绑定本机图。
- 支持从云端地图库绑定占位场景。
- 云读取到 placeholder 后自动提示下一步。
- 恢复入口锁回编辑态,避免跑团态暴露维护工具。

### v3.18.0

云端地图库 polish 和朋友电脑恢复提示增强。

主要变化:

- 支持可选 `manual-maps/index.json`。
- 可提前显示云端大图尺寸、文件大小和备注。
- 绑定时仍校验真实图片尺寸。
- 补充只读钥匙、占位地图、Checkpoint 边界提示。
- 明确 Checkpoint 只恢复两层迷雾。

### v3.17.0

新增词条 A-Z 快捷键。

主要变化:

- 新建和管理词条时可绑定英文字母。
- 跑团中按键临时切换词条聚光。
- 快捷键不会永久修改遮罩或对象层级。
- 输入框打字时不触发词条快捷键。

### v3.16.0

新增云端地图库绑定。

主要变化:

- 可把超大地图手动放入私有仓 `manual-maps/`。
- 读取占位场景后,从云端选择同尺寸图片绑定。
- 上传云存档时只保存引用,不重复上传这类大图。
- 支持 `mapSource.type = 'manualRemote'`。

### v3.15.0

新增 PC 鼠标空格安全选择层。

主要变化:

- 按住空格点击对象或空白,不擦迷雾。
- 按住空格拖动仍平移视图。
- 补充 iPad 触控优化待办。

### v3.14.0

新增词条聚光。

主要变化:

- 事件、Token、文字标注、图片标注可挂词条。
- 跑团态选择词条后,临时显露和高亮同词条对象。
- 删除旧演示 / 隐藏 UI 入口。
- 激活词条不会永久改迷雾或层级。

### v3.13.0

云存档支持超大地图占位恢复,并新增编辑态事件快切。

主要变化:

- 真正太大的地图不上传图片本体。
- 仍同步遮罩、Token、事件、标注和地图尺寸。
- 另一台电脑读取后可绑定同尺寸原图。
- 普通图和可优化地图继续上传。
- 编辑态新增事件快切。

### v3.12.2

云存档地图与状态分片降到更保守大小,降低 GitHub API 失败概率。

### v3.12.1

云上传大地图时使用工具工作尺寸的优化副本,减少原图体积压力。

### v3.12.0

云存档支持大状态 / 大地图分片、上传取消、pending 草稿和云端清理。

主要变化:

- 大状态和地图分片上传。
- 上传中可取消。
- 上传中断后保留 pending 草稿提示。
- 可清理云端孤儿地图和老快照。

### v3.11.0

云存档增加历史快照。

主要变化:

- 查看历史进度。
- 恢复指定快照。
- 锁定重要快照。
- 识别只读钥匙。
- 上传冲突和 pending 草稿提示增强。

### v3.10.0

云存档实现上传当前进度和读取云端最新进度。

主要变化:

- 上传当前全部场景状态。
- 读取云端最新快照。
- 状态数据 gzip、base64 并写入私有仓。
- 普通地图图片可随云存档保存。

### v3.9.0

云存档基础框架。

主要变化:

- 输入 GitHub fine-grained PAT。
- 验证私有仓访问。
- 本机信任和设备 ID。
- 云入口加入更多菜单。

### v3.8.1

底栏章节信息可编辑。

主要变化:

- 每个场景可保存章节序号。
- 每个场景可保存章节标题。
- 当前地图铭牌居中增强。
- 章节信息随场景导出 / 导入。

### v3.8.0

接入黑曜石 / 黄铜视觉系统。

主要变化:

- 重做顶栏、底栏、按钮和面板质感。
- 使用纯 CSS 风格,不引入外部图片资源。
- 保持地图为视觉中心。

### v3.7.1

编辑态对象侧栏增强。

主要变化:

- 增加搜索。
- 增加类型筛选。
- 增加定位入口。
- 增加显式编辑入口。
- 审计全部场景导出内容。

### v3.7.0

建立跑团态 / 编辑态切换。

主要变化:

- 默认跑团态。
- 编辑态展开完整工具。
- 跑团态收回准备工具。
- 编辑态新增对象侧栏预览。

### v3.6.0

新增全部场景导出 / 导入向导,并新增方形网格。

主要变化:

- 可导出多场景 bundle。
- 导入时逐场景绑定本机地图。
- 支持方形网格。

### v3.5.0

新增地图标注和全揭笔刷。

主要变化:

- 文字标注。
- 图片标注。
- 标注可选择公开、受地形遮罩或受事件遮罩。
- 全揭笔刷同时揭开地形层和事件层。

### v3.4.0

确认共享屏幕产品设计。

主要变化:

- 明确工具是玩家全程看得到的共享桌面工具。
- 默认跑团态。
- 编辑态作为准备和维护层。
- 不做 Player Mode。

### v3.3.2

修复全部场景导入 / 读取后遮罩错位和卡顿问题。

### v3.3.1

六边形网格支持旋转校准。

### v3.3.0

六边形网格支持位置和大小校准,便于对齐原图网格。

### v3.2.1

修复网格设置导出 / 导入丢失问题。

### v3.2.0

事件可选择显示在遮罩上层或受事件雾遮罩。

### v3.1.0

新增六边形网格遮罩笔刷。

### v3.0.0

新增多场景管理。

主要变化:

- 每个场景独立保存地图。
- 每个场景独立保存迷雾。
- 每个场景独立保存事件、Token 和设置。
- 可在多个地图之间切换。

### v2.5.0

新增导出 / 导入,支持转移场景进度。

### v2.4.0

事件层默认透明度调整,便于 DM 区分两层迷雾。

### v2.3.0

地形层迷雾颜色可配置。

### v2.2.0

事件层迷雾颜色可配置。

### v2.1.0

新增事件层,与地形迷雾分离。

### v2.0.0

新增事件系统、Token、Checkpoint 和更完整的 DM 操作流。

### v1.3

单层战争迷雾基础版。
